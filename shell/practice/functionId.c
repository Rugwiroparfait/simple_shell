#include <unistd.h>
#include <stdio.h>

/**
 * get_processId: finds The process Id
 * @pid: keeps the process Id
 *
 * Return : the processId
 */

void get_processId (pid_t pid)
{
	/**the function must be the prototype of the maincodes**/
	/**get_processId must do all the work instead**/

	return (getpid());
}

/**
 * addition: finds the summation
 * num1 : the first number to be added
 * num2 : the second number tobe added
 *
 * Return : summation at success, -1 on failure
 */

void addition(unsigned int num1, unsigned int num2)
{
unsigned int summation;
summation = num1 + num2;

return (summation);
}

int main (void)
{
	print
